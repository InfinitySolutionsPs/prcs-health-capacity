services:
  prcs-health-capacity:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "3000"
    env_file:
      - /opt/prcs-health/prcs-health-capacity/.env
    volumes:
      - /opt/prcs-health/prcs-health-capacity/data:/app/data
