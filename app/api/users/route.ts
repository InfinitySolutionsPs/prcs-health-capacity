import {NextResponse} from "next/server";import {getRawDb} from "@/db";import {requireRole,Role} from "@/lib/authorization";
export async function GET(){if(!await requireRole(["admin"]))return NextResponse.json({error:"هذه العملية للمدير فقط"},{status:403});const r=await getRawDb().prepare("SELECT id,email,name,role,active,created_at AS createdAt FROM system_users ORDER BY name").all();return NextResponse.json({users:r.results})}
export async function POST(req:Request){if(!await requireRole(["admin"]))return NextResponse.json({error:"هذه العملية للمدير فقط"},{status:403});try{const b=await req.json(),email=String(b.email||"").trim().toLowerCase(),name=String(b.name||"").trim(),role=String(b.role||"") as Role;if(!email.includes("@")||name.length<2||!["admin","editor","viewer"].includes(role))return NextResponse.json({error:"أدخل بيانات المستخدم كاملة"},{status:400});const r=await getRawDb().prepare("INSERT INTO system_users (email,name,role,active) VALUES (?,?,?,1)").bind(email,name,role).run();return NextResponse.json({id:r.meta.last_row_id,email,name,role,active:1},{status:201})}catch(e){console.error(e);return NextResponse.json({error:"البريد مستخدم مسبقًا أو تعذر الحفظ"},{status:409})}}
