"use client";
import {useState} from "react";
export const dynamic="force-dynamic";
export default function LoginPage(){const[setup,setSetup]=useState(false),[checked,setChecked]=useState(false),[username,setUsername]=useState(""),[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);useState(()=>{fetch("/api/auth/setup").then(r=>r.json()).then(b=>{setSetup(Boolean(b.needsSetup));setChecked(true)}).catch(()=>setChecked(true))});async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");if(setup&&password!==confirm){setError("كلمتا المرور غير متطابقتين");setBusy(false);return}try{const endpoint=setup?"/api/auth/setup":"/api/auth/login";const r=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username,password})});const b=await r.json();if(!r.ok)throw new Error(b.error||"تعذر إتمام العملية");location.href="/"}catch(e){setError(e instanceof Error?e.message:"تعذر إتمام العملية");setBusy(false)}}return <main dir="rtl" className="grid min-h-screen place-items-center bg-[#f3f6f8] p-5 text-right"><section className="w-full max-w-md rounded-3xl border border-[#dfe5e9] bg-white p-8 shadow-lg"><div className="mb-6 flex items-center gap-4"><span className="grid size-20 place-items-center rounded-full bg-white p-1 shadow"><img src="/prcs-logo.png" alt="شعار الهلال الأحمر الفلسطيني" className="size-full object-contain"/></span><div><h1 className="text-2xl font-extrabold">{setup?"إعداد النظام لأول مرة":"تسجيل الدخول"}</h1><p className="mt-2 text-sm text-[#6b7681]">نظام القدرة التشغيلية لقطاع الصحة</p></div></div>{!checked?<p className="py-6 text-center">جارٍ تجهيز النظام...</p>:<form onSubmit={submit} className="space-y-4"><label className="block"><span className="mb-2 block font-semibold">اسم المستخدم</span><input required minLength={3} value={username} onChange={e=>setUsername(e.target.value)} className="h-12 w-full rounded-xl border px-4" autoComplete="username"/></label><label className="block"><span className="mb-2 block font-semibold">{setup?"كلمة المرور الجديدة":"كلمة المرور"}</span><input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} className="h-12 w-full rounded-xl border px-4" autoComplete={setup?"new-password":"current-password"}/></label>{setup&&<label className="block"><span className="mb-2 block font-semibold">تأكيد كلمة المرور</span><input required minLength={8} type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} className="h-12 w-full rounded-xl border px-4" autoComplete="new-password"/></label>}{error&&<p className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={busy} className="h-12 w-full rounded-xl bg-[#a50f27] font-bold text-white disabled:opacity-60">{busy?(setup?"جارٍ إنشاء الحساب...":"جارٍ الدخول..."):(setup?"إنشاء حساب المدير":"تسجيل الدخول")}</button></form>}</section></main>}
